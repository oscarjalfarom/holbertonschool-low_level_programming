#include "main.h"

/**
 * _abs - that computes the absolute value of an integer
 * @n: number with value
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
